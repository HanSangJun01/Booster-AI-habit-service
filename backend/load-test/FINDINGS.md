# A축 성능/오류 문제 목록 (BS-30) — 이번 주 결과물

> 부하 테스트 + Grafana 관찰로 발견한 "느린 곳 / 터지는 곳"을 여기에 누적 기록.
> **이번 주는 기록만.** 고치는 건 다음 주 A축 원본 브랜치에서. ("금방 고치겠는데?" 싶어도 여기 적고 넘어가기)

## 테스트 환경 메모
- 일시: 2026-06-25 (1차 측정)
- 부하 프로필: ramping-vus 0→20→50→100, 약 3분 (a-axis-load-test.js 기준)
- DB 데이터량(대략): 빈 DB에서 시작(Flyway 체크섬 충돌로 볼륨 재생성 후). 부하용 유저 1명 + 위치/체크인 데이터 없음
- 전체 결과: 34,187 요청 / 180 req/s / 실패율 19.99%(아래 E1 참고) / 읽기 p95 149ms

---

## 발견 목록

### 🐢 느린 곳 (성능)
| 엔드포인트 | 평균 | p95 | p99 | 부하 올릴 때 변화 | 추정 원인(힌트) |
|-----------|------|-----|-----|------------------|----------------|
| 전체 읽기 API(5종 합산) | 45ms | 149ms | — | 100명에서도 안정 | 없음(목표 500ms 대비 양호) |

- **성능은 현재 문제 없음.** 100 VU 고부하에서도 읽기 p95 149ms로 목표(500ms) 한참 아래.
- 다만 `max=1.02s` 단발 스파이크 관찰됨 → 콜드스타트/일시 GC 추정. 재현 빈도 낮음. 2차 측정에서 p99 패널로 재확인 필요(현재 k6 요약엔 p99 미표기, Grafana에서 확인).

### 💥 터지는 곳 (오류)
| ID | 엔드포인트 | 상태코드 | 언제 발생 | 빈도 | 성격 |
|----|-----------|---------|----------|------|------|
| E1 | GET /api/users/me/location | 404 (LOCATION_NOT_FOUND) | 매 요청 | 6837/6837 (100%) | **제품 버그 아님 — 테스트 셋업 빈틈** |

- **E1 해석:** 부하 스크립트 setup()이 유저 가입·로그인만 하고 **위치 등록(POST /location)을 안 함** → 위치 없는 유저가 GET을 부르면 서비스가 의도대로 404 반환(`PersonalLocationService.get` 42~45줄). GlobalExceptionHandler가 404로 정상 처리, **500/서버 크래시 0건**.
- 즉 전체 실패율 19.99%는 이 한 엔드포인트의 정당한 404가 만든 수치(5개 중 1개 = 20%). **서버 안정성 문제 아님.**
- 나머지 4개 엔드포인트(`/dashboard/home`, `/users/me/coins`, `/personal/check-in/today`, `/personal/recovery/status`)는 100% 200 OK.
- **측정 정확도 개선 과제(다음 주):** load-test setup()에서 위치를 등록하도록 보강해야 이 엔드포인트의 "진짜 성능"이 측정됨. (현재는 404 빠른 반환이라 빠르게 보이는 착시 가능)

### 🔌 자원 병목 (DB풀 / 메모리 등)
| 증상 | 관찰값 | 관련 엔드포인트 |
|------|--------|----------------|
| 이번 측정에선 Hikari pending/SQL_SLOW 뚜렷한 신호 미확인 | Grafana 관찰값 기록 안 됨 | — |

- 2차 측정 시 Grafana "Hikari pending" 패널과 백엔드 콘솔 `SQL_SLOW`/`generate_statistics`(요청당 쿼리 수)를 의식적으로 캡처할 것. 특히 `/dashboard/home`은 여러 도메인 집계라 N+1 가능성 점검 대상.

---

## 다음 주 개선 후보 (우선순위)
1. **(측정 보강, 선행)** load-test setup()에 위치 등록 추가 → `/users/me/location` 실제 성능 측정 가능하게. (E1 착시 제거)
2. **(점검)** `/dashboard/home` 쿼리 수/N+1 여부 확인 — 빈 DB라 빨랐을 뿐, 데이터 쌓이면 느려질 수 있음. 시드 데이터로 재측정.
3. **(관찰)** `max=1.02s` 단발 스파이크 원인 추적(콜드스타트 vs GC) — p99 기준으로 재확인.
