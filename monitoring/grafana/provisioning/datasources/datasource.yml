# [BS-30] Grafana가 시작하면서 Prometheus를 데이터소스로 자동 등록 (수동 설정 불필요)
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus          # 대시보드 패널이 이 uid로 참조함
    access: proxy
    url: http://prometheus:9090   # 같은 compose 네트워크 내 서비스명
    isDefault: true
    editable: true
